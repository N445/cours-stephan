{% extends 'base-checkout.html.twig' %}

{% set title = 'Mode de réglement'%}

{% block checkoutBody %}
    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}

            <div class="d-flex">
                <a href="{{ path('APP_CHECKOUT_ADDRESS') }}" class="btn btn-secondary">
                    Retour
                </a>
                <button type="submit" class="btn btn-primary ms-3">
                    Payer
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
