{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    {{ component('TopBannerComponent',{
        'title':title
    }) }}
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                {% block checkoutBody %}
                {% endblock %}
            </div>
            <div class="col-md-6">
                {{ component('RecapCart',{
                    initialFormData:cart
                }) }}
            </div>
        </div>
    </div>
{% endblock %}
